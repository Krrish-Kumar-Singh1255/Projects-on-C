# 🔐 User Management System in C

A simple **C program** that allows users to **register** and **log in** using a username and masked password.  
This project demonstrates user authentication and input handling in C.

---

## 🧠 Overview

This program:
- Lets users **register** with a username and password  
- Allows users to **log in** using their credentials  
- Masks password input for security using `_getch()`  
- Limits the number of users to prevent overflow  

---

## ⚙️ Features

✅ Register new users (up to 5)  
✅ Secure login with masked password  
✅ Validation for invalid inputs  
✅ Prevents buffer overflow  
✅ Runs entirely in the console  

---

## 🧾 Example Output

